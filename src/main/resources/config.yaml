database:
  url: {{POSTGRES_URL}}/tradingbotdb
  user: tradingbot
  password: admin123

bot:
  symbol: "BTCUSDC"
  name: leverage-btcusdc-1h-20d
  core_interval: 1800
  amount_to_trade: 20

  strategy:
    type: LEVERAGE
    baseDifference: 0.03
    targetMultiply: 1.7
    targetPricePercentage: 0.004   #next price to buy

binance:
  url: {{BINANCE_URL}}
  api_key: {{API_KEY}}
  secret_key: {{SECRET_KEY}}
